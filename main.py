import pandas as pd


def main():
    """
    Main function
    """
    print("Hello World")
    df.str.replace("\n", "").str.replace("\n", "").str.replace("\n", "").str.replace(
        "\n", ""
    ).str.replace("\n", "").str.replace("\n", "").str.replace("\n", "").str.replace(
        "\n", ""
    ).str.replace(
        "\n", ""
    ).str.replace(
        "\n", ""
    )
